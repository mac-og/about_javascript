// import {circleArea, trapeziumArea} from './calShape.js' // when impoting only named functions
import * as cal from './calShape.js' // used to import all the exports from a file

let total = cal.circleArea(20);
console.log(total);