/*  A prompt provides a response field for a user to input data.
    It also makes use of a default value in case no response is given
*/

// if no value is given then 'undefined' is atored
let lang = window.prompt('What language do you prefer ?');

const prefered = prompt('Preferd langauge ?', 'javaScrpt');